#include "stdafx.h"

using namespace PManager;
using namespace std;

ProjectHandle * LoadProject(string path)
{
	
}

ProjectHandle ProjectManager::NewProject(std::string name)
{
	ProjectHandle handle;

	//check if project exists

	return handle;
}

ProjectHandle ProjectManager::LoadProject(std::string name)
{
	ProjectHandle handle;

	//check if project exists

	return handle;
}
